{{>header}}
{{>navbar}}
<section class="my-3 container">
    <form method="post">
        <div class="mb-3">
            <input class="form-control" placeholder="{{user.id}}" disabled name="id" value="{{user.id}}">
        </div>
        <div class="mb-3">
            <input class="form-control" placeholder="user name" value="{{user.name}}" name="userName">
        </div>
        <div class="mb-3">
            <input class="form-control" placeholder="user age" value="{{user.age}}" name="age">
        </div>
        <div class="mb-3">
            <input type="submit" class="btn btn-primary" value="add user">
        </div>
    </form>
</section>
{{>footer}}